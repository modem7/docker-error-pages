kind: pipeline
type: docker
name: deploy

platform:
  os: linux
  arch: amd64

steps:
  - name: deploy
    image: thegeeklab/drone-docker-buildx
    privileged: true
    settings:
      repo: modem7/error-page-test
      # dry_run: true  # disable when not testing
      purge: true
      compress: true
      use_cache: true
      cache_from: "modem7/error-page-test:latest"
      platforms:  # if it doesn't work run docker run --privileged --rm tonistiigi/binfmt --install all
        - linux/amd64
      username:
        from_secret: docker_username
      password:
        from_secret: docker_password
      tags:
        - latest

  - name: slack
    image: themaz/drone-slack
    settings:
      webhook:
        from_secret: slack_hook
    when:
      status: [ success, failure ]

trigger:
  event:
    - custom